create posts
    add user filter
container for front
Are the migrations being applied by the migration system?
notifications
    click out - close pop-up
left side panel
    click out
friend request
    notifications might keep the previous user session??? wtf???
profile page
    remove eye toggle button
    add throttle/confirm to public/private
    if private user (not following), show only avatr and name
posts
    fix hide private
    fix edit comment bad request 404
    fix delete comment bad request 404
groups
    fix create groups bad request 400
    fix request
    in posts, cant see the username who submitted it
    add emojis in group chat
    events
        create event - fix enter time
chat
    have a notification in minimized chat